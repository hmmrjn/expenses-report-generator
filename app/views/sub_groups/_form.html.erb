<%= form_with(model: sub_group, local: true) do |form| %>
  <% if sub_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_group.errors.count, "error") %> prohibited this sub_group from being saved:</h2>

      <ul>
      <% sub_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.select :group_id, options_from_collection_for_select(@groups, :id, :name, selected: (sub_group.group)? sub_group.group.id : (params[:group_id])? params[:group_id] : nil) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
